@model IEnumerable<LectoresConGloria_MDL.Vistas.V_Lista>

<h3>Categoria</h3>
<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-md-3" ng-repeat="Type in Types">
            @Html.ActionLink(item.Valor, "", "TextoCategoria",, null)
        </div>
    }
</div>
