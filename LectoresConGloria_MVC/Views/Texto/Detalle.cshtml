@model LectoresConGloria_MDL.Vistas.V_TextoDetalle

@{
    ViewData["Title"] = "Detalle";
}

<h1>Detalle</h1>

<div>
    <h4>@Model.Titulo</h4>
    <hr />
    <dl class="row">
        @if (Model.Texto)
        {
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Texto)
            </dt>
            <dd class="col-sm-10">
                @Html.ActionLink("Ver", "Goto", "Click", new { /* id = Model.PrimaryKey */ }, new { target = "_blank" })
            </dd>
        }
        @if (Model.Audio)
        {
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Audio)
            </dt>
            <dd class="col-sm-10">
                @Html.ActionLink("Ver", "Goto", "Click", new { /* id = Model.PrimaryKey */ }, new { target = "_blank" })
            </dd>
        }
        @if (Model.Audio)
        {
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Explicacion)
            </dt>
            <dd class="col-sm-10">
                @Html.ActionLink("Ver", "Goto", "Click", new { /* id = Model.PrimaryKey */ }, new { target = "_blank" })
            </dd>
        }
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Vistas)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Vistas)
        </dd>
    </dl>
</div>
