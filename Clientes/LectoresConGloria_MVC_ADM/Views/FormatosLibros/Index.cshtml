@model IEnumerable<LectoresConGloria_MVC_ADM.Models.TBL_FormatosLibros>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TBL_Formatos.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TBL_Libros.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Archivo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TBL_Formatos.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TBL_Libros.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Archivo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
